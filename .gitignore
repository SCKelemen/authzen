# Compiled binaries and the local CLI build (cmd/authzen).
/authzen
/cmd/authzen/authzen
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binaries produced by `go test -c`.
*.test

# Output of the Go coverage tool.
coverage.out
coverage.txt
*.cover
*.out
profile.cov

# Go workspace files (kept local; this repo uses module-local replace).
go.work
go.work.sum

# Dependency vendor directory (we do not vendor).
/vendor/
/grpc/vendor/

# Editor / OS noise.
.DS_Store
.idea/
.vscode/
*.swp
*~

# NOTE: the generated protobuf/gRPC stubs under grpc/gen/ are intentionally
# committed and MUST NOT be ignored. CI regenerates them and fails if they
# drift, so they stay tracked.

# Local AI tool config (Slate / Random Labs) — kept on disk, not tracked.
slate.json
